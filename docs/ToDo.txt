+ Unificar s3vz con mi spong
+ Camara tapo, un plugin qu verifique que responde el streaming   [DONE v3.5.0]
+ s3 graficos rrd disco para todas las particiones, ahora al parecer hacer los graficos de cada particion por separado por eso no se ven, que las junte en un solo grafico
 + Ver de Sacar Max/Min/Avg/etc...
+ historial reves y filtros para servicio y estado
 + en Historial (http://s.unsl.edu.ar/spong/history) Ver de poder hacer multi seleccion del estado filtro, por ejemplo Red+Green
+ cpu graficos agregar carga y no jobs
+ Ver de poner horario a eventos que marque con rojo, por ejemplo carga cpu eco1 por la manana que lo vea pero no ponga en rojo de lunes 1 viernes de 7:30hs a 16:00hs   [DONE v3.5.0]
+ Front-end configuracion hosts.yaml / groups.yaml para gente con poco conocimiento   [DONE v3.5.0]
+ ver pq no anda graficos de cpu
+ revision de que este en todos lo idiomas completos y que incluyan le edicion de config y tambien modo claro/oscuro en config
+ modifica para poder tener la clave encriptada en la config
+ cuando en config se apunta a "SPONG Configuracion", hacer que se muestre SPONG vx.x.x - creado por mt (igual que en spong)
+ hacer que tenga niveles de usuarios para config, por ejemplo uno que solo pueda agregar sin poder editar/borrar
+ en config -> Host, sale un servicion ".py" , no tiene que estar
+ en el spong, si presiono sobre el nombre del grupo poder minimizar/ampliar la vista de todo el grupo y que por defecto el grupo esta como minimizado si esta en verde, pero si esta en rojo/amarillo/violeta este ampliado
+ tener muchos usuarios para config, y que se registre cuando modifican algo lo que modifican
+ el user admin tambien tiene que poder config los usuarios/claves/roles
+ agregar opcion de salir de user en config
+ modificar para en spong sobre "Grupos de Hosts" poder apretar para minimizar/ampliar/(minimizado si es verde-maximizado rojo/amarillo/violeta) todos los grupos
+ en "Grupos de Hosts" el toggle ahora cicla 4 modos (rojo expandido / problemas expandidos / todos expandidos / todos minimizados); por defecto solo se expanden los grupos en rojo
+ la barra lateral izquierda (lista de problemas) ahora queda sticky al scrollear el contenido principal del dashboard
+ el título de página ("Grupos de Hosts" en el dashboard, y los títulos equivalentes en problemas/host/servicio/etc.) queda sticky debajo del header al scrollear
+ desde config, solo poner o cambiar clave de usuarios con texto en el frond-end(no hash), que luego queda guardado el hash en la config
+ en config/Hosts poder ordenar por Host/IP  de Menor a Mayor o viceversa
+ en config/Grupos poder ordenar por Clave/Nombre de Menor a Mayor o viceversa
+ En los graficos de temperaturas, cuando es por dia, mes y año, hacer graficos de maximo y minimo de cada hora/dia/mes, como se ve en /usr/local/spong/tmp/temp.png
+ desde el host del spong poder ir a modificar los servicios directamenta de la config si el usuario se tiene los permisos necesarios
+ hay un bug, si tengo un servicio en rojo, y lo borro con el config, este sigue apareciendo como rojo
+ actualizar version a v3.5.1 , actualizar documentacion, generar .deb v3.5.1, y subir a github  [DONE v3.5.1]
+ hacer click en "SPONG" (superior izquierda), sea lo mismo que persionar en "Grupos"  [DONE v3.5.2]
+ cuando borro un azul en reconocidos, en vista principal vuelve el estado (Verde/Rojo/Amarallo/Violeta), pero sigue estando en la lista de "Reconocidos"  [DONE v3.5.2]
+ actualizar version a v3.5.2, actualizar documentacion, generar .deb v3.5.2, y subir a github  [DONE v3.5.2]
+ ver dejar modificar host desde config (ahora hay que borrar y volver a crear)  [DONE v3.5.3]
+ actualizar version a v3.5.3, documentacion, .deb y github  [DONE v3.5.3]
+ hacer multi-user en spong como en config  [DONE v3.5.4]
+ en el frond-end controlar que los nombres de hosts no tengan caracteres raros como /  [DONE v3.5.4]
+ actualizar version a v3.5.4, documentacion, .deb y github  [DONE v3.5.4]
+ cuando se va desde un hosts a "Editar servicios", se modifica y guarda, vuelta al spong donde se estaba antes de editar  [DONE v3.5.5]
+ rediseno de la pagina de "Permiso insuficiente" en monitor y en /config  [DONE v3.5.5]
+ permitir a usuarios con rol editor reconocer problemas y borrar reconocimientos  [DONE v3.5.5]
+ actualizar version a v3.5.5, documentacion, .deb y github  [DONE v3.5.5]
+ rediseno de la pagina 401 (Basic Auth) en monitor y en /config con estilo claro/oscuro  [DONE v3.5.6]
+ actualizar version a v3.5.6, documentacion, .deb y github  [DONE v3.5.6]
+ separar plugins de network del .deb del server / overrides de plugins que sobrevivan al upgrade  [DONE v3.5.7]
+ actualizar version a v3.5.7, documentacion, .deb y github  [DONE v3.5.7]
+ ver que la barra de la izquierda (indicando los problemas) no se baje cuando paso para abajo la parte de la derecha
+ Integrar con SGT (Sistema de Gestión de Tareas UNSL): botón en /problems y /host para crear tickets, dedup en var/sgt_links.json, sync periódico (auto-resolver al volver verde, limpieza si se cierra en SGT)   [DONE v3.5.11]
+ en el historial de un host la hora sale con distinto utc del sistema  [no era bug: la hora del historial es local -03 correcta; el desfase venía del reloj JS del navegador]
+ cuando se crea un ticket y sale el numero en el spong, se puede hacer que quede en el historial del host y general quede una referencia al ticket por mas que el ticket ya fue cerrado?  [DONE v3.6.0]
+ agrega filtro por estado/servicio en el historial de un host  [DONE v3.6.0]
+ actualizar version a v3.6.0, documentacion, .deb y github  [DONE v3.6.0]
+ plugins SNMP: cuando falla la lectura SNMP devolver violeta en lugar de rojo (suptime, scpu*, rcpu, rtemp, macs, memolt, interfaces, iftraffic, ups)  [DONE v3.6.1]
+ actualizar version a v3.6.1, documentacion, .deb y github  [DONE v3.6.1]
+ en la sidebar izquierda, problemas con ticket SGT activo se muestran en bordeaux para distinguir del rojo "sin ticket"  [DONE v3.6.2]
+ actualizar version a v3.6.2, documentacion, .deb y github  [DONE v3.6.2]
+ fix plugins: https (expiracion de cert era codigo muerto por CERT_NONE + backend colgado tras handshake reportaba verde), dns (consultaba resolver local en vez del host objetivo), processes (enviaba servicio "jobs"), ups temp_ext (rojo falso a 25C por umbral /10 inconsistente), safe_exec (LC_ALL=C para parsing en locale es)  [DONE v3.6.6]
+ actualizar version a v3.6.6, documentacion, .deb y github  [DONE v3.6.6]
+ seguridad servidor: path traversal por hostname ".."/"." (protocol.valid_host + handler BB + guards en database/rrd/ruta web /rrd), save_status atomico (temp+os.replace) con lock por (host,service), _update_ping ya no borra el RRD ante fallo transitorio de rrdtool info (-1 vs 0), gate de permiso "users" en /config/history (view/restore/listado)  [DONE v3.6.7]
+ actualizar version a v3.6.7, documentacion, .deb y github  [DONE v3.6.7]
+ proteccion CSRF (double-submit cookie con token en cookie HttpOnly + campo oculto/meta + hmac.compare_digest) en el monitor (/ack, /sgt-ticket, /api/check via header) y en todo el panel /config (alta/edit/borrado hosts/usuarios/grupos, rename, restore)  [DONE v3.6.8]
+ actualizar version a v3.6.8, documentacion, .deb y github  [DONE v3.6.8]
+ escritura atomica de YAML de config (_save_yaml/_save_config_snapshot/log.json con tmp unico via mkstemp + fsync + lock por ruta) y de sgt_links.json (tmp unico + fsync + flock cross-process web/spong-sgt-sync; dedup de crear_ticket bajo lock evita tickets duplicados por doble clic; sync_once minimiza tiempo bajo lock)  [DONE v3.6.9]
+ actualizar version a v3.6.9, documentacion, .deb y github  [DONE v3.6.9]
+ dashboard TUI en terminal (spong top / spong-tui): vista curses en vivo que lee var/database + etc/*.yaml (misma fuente que la web), arbol grupos->hosts con circulos de color, panel de servicios con detalle/acks/historial, auto-refresh 10s, navegacion con flechas; robusto ante terminales chicas y resize  [DONE v3.7.0]
+ actualizar version a v3.7.0, documentacion, .deb y github  [DONE v3.7.0]
+ el .deb del server instala alias global 's' en /usr/local/bin/ -> spong-tui (tipear s abre el dashboard TUI)  [DONE v3.7.1]
+ actualizar version a v3.7.1, documentacion, .deb y github  [DONE v3.7.1]
+ documentar auditoria: informe consolidado en docs/auditoria-2026-07.md (60 hallazgos, 22 corregidos v3.6.6-v3.7.1, pendientes), seccion "Seguridad y robustez" en README (§13b), docstrings en helpers nuevos (CSRF, locks, escritura atomica)  [DONE]
+ TUI: la tecla H muestra el historial del SERVICIO seleccionado (cortes/regresos) como la web, y alterna servicio->host->cerrar; antes solo mostraba el historial del host mezclado  [DONE v3.7.2]
+ actualizar version a v3.7.2, documentacion, .deb y github  [DONE v3.7.2]
